device_subproject_deps = \
    riscv

device_hdrs = \
    uart.h \
    plic.h \
    sdhc.h

device_CFLAGS = -fPIC

device_srcs = \

ifeq ($(zjv_device_extension),yes)
riscv_zjv_device_extension += \
	uart.cc \
	plic.cc
endif

